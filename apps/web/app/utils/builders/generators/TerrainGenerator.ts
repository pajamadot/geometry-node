import * as pc from 'playcanvas';
import { EnhancedGeometryData } from '../GeometryBuilder';

export class TerrainGenerator {
    static generate(width: number, height: number, subdivisions: number): EnhancedGeometryData {
        // Placeholder
        return {} as EnhancedGeometryData;
    }
}
