import * as pc from 'playcanvas';
import { EnhancedGeometryData } from '../GeometryBuilder';

export class LODGenerator {
    static generate(geometry: EnhancedGeometryData, count: number): EnhancedGeometryData[] {
        // Stub implementation
        return [geometry];
    }
}
