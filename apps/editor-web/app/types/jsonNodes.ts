// Re-export shim — the canonical definitions now live in the shared, Workers-safe
// package @geometry-script/agent-core. Editor-web keeps importing from this path.
export * from '@geometry-script/agent-core/jsonNodes';
